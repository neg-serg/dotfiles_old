/* Fallback Mode Panel */
PanelWidget,
PanelTop,
PanelApplet,
PanelToplevel {
 background-image: -gtk-gradient (linear, left top, right top,
                                     from (alpha (#525252, 1.0)),
                                     to (alpha (#525252, 1.0)));

	padding: 0;
    border-radius:3;
    color: #fff;
    text-shadow: 0 1 shade (#000, 0.6);
}

.gnome-panel-menu-bar{

    -unico-glow-color: shade (#000, 1.0);
    -unico-glow-radius: 6;
 background-image: -gtk-gradient (linear, left top, right top,
                                     from (alpha (#525252, 1.0)),
                                     to (alpha (#525252, 1.0)));


    -unico-border-gradient: -gtk-gradient (linear, left top, left bottom,
                                           from (alpha (#525252, 1.0)),
                                           to (alpha (#525252, 1.0)));
    -unico-inner-stroke-gradient: -gtk-gradient (linear, left top, left bottom,
                                           from (alpha (#525252, 1.0)),
                                           to (alpha (#525252, 1.0)));   
    border-radius:0;
    border-width:1;
    color: #fff;
    text-shadow: 0 1 shade (#000, 0.6);
}
PanelApplet > GtkMenuBar.menubar,
PanelApplet > GtkMenuBar.menubar.menuitem,
PanelMenuBar.menubar,
PanelMenuBar.menubar.menuitem {

    -unico-glow-color: shade (#000, 1.0);
    -unico-glow-radius: 6;
 background-image: -gtk-gradient (linear, left top, right top,
                                     from (alpha (#525252, 1.0)),
                                     to (alpha (#525252, 1.0)));


    -unico-border-gradient: -gtk-gradient (linear, left top, left bottom,
                                           from (alpha (#525252, 1.0)),
                                           to (alpha (#525252, 1.0)));
    -unico-inner-stroke-gradient: -gtk-gradient (linear, left top, left bottom,
                                           from (alpha (#525252, 1.0)),
                                           to (alpha (#525252, 1.0)));   
    border-radius:0;
    border-width:1;
    color: #fff;
    text-shadow: 0 1 shade (#000, 0.6);
}

PanelAppletFrame { 
     background-image: -gtk-gradient (linear, left top, left bottom,
                                     from (alpha (#525252, 1.0)),
                                     to (alpha (#525252, 1.0)));
	color: @selected_fg_color;
	text-shadow: none;
	color: @selected_fg_color;
}

PanelApplet .button {

    border-radius:10 10 0 0;

    color: #666;
    text-shadow: 0 1 shade (#dbdbdb, 0.99);
}

PanelApplet .button:prelight:active,
PanelApplet .button:active {
    -unico-inner-stroke-gradient: -gtk-gradient (linear, left top, left bottom,
                                           from (alpha (#363636, 0.4)),
                                           to (alpha (#363636, 0.4)));  

    -unico-glow-color: shade (#000, 1.0);
    -unico-glow-radius: 0;
    border-radius:10 10 0 0;
    color: #363636;
    text-shadow: 0 1 shade (#fff, 0.6);
}

PanelApplet .button:prelight {
    border-radius:10 10 0 0;
    color: #fff;
    text-shadow: 0 1 shade (#000, 0.6);
}

ClockBox {

    color: #363636;
    text-shadow: 0 1 shade (#fff, 0.9);

}

WnckPager, WnckTasklist {
     background-image: -gtk-gradient (linear, left top, left bottom,
                                     from (alpha (#525252, 1.0)),
                                     to (alpha (#525252, 1.0)));
	color: @selected_fg_color;
	text-shadow: none;
}

NaTrayApplet {
	-NaTrayApplet-icon-padding: 2;
	-NaTrayApplet-icon-size: 20;
	color: #525252;
}
