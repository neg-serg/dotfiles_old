font pango:PragmataPro for Powerline 12

# enable-ssh-support
# default-cache-ttl-ssh  800
# max-cache-ttl-ssh      800

bindsym $mod+Shift+o exec quickswitch.py -s

set workspace_layout tabbed

set $mod Mod4
set $alt Mod1
set $Locker i3lock && sleep 1
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
set shmlog on

set $soft_restart killall -SIGUSR1 i3status

set_from_resource $darkred     color1  #000000
set_from_resource $red         color9  #000000
set_from_resource $darkgreen   color2  #000000
set_from_resource $green       color10 #000000
set_from_resource $darkyellow  color3  #000000
set_from_resource $yellow      color11 #000000
set_from_resource $darkblue    color4  #000000
set_from_resource $blue        color12 #000000
set_from_resource $darkmagenta color5  #000000
set_from_resource $magenta     color13 #000000
set_from_resource $darkcyan    color6  #000000
set_from_resource $cyan        color14 #000000
set_from_resource $darkwhite   color7  #000000
set_from_resource $white       color15 #000000

set $ws1   1:term
set $ws2   2:web
set $ws3   3:doc
set $ws4   4:dev
set $ws5   5:media
set $ws6  6:gimp
set $ws7  7:admin
set $ws8  8:ide
set $ws9   9:steam
set $ws10  10:torrent
set $ws11 11:vm
set $ws12 12:wine

# Initialize workspace
#                     

#workspace $ws1 gaps inner 10
#workspace $ws1 gaps inner 10

gaps inner 20
gaps outer 5

smart_gaps on
smart_borders on

for_window [class="^.*"] border pixel 1
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [title="Preferences$"] floating enable

# set $psst --no-startup-id

for_window [instance="mpd-pad2"] move scratchpad, move absolute position 680 0, resize set 1200 600
bindsym $mod+f [instance="^mpd-pad2$"] scratchpad show

for_window [class="^(skypeforlinux|TelegramDesktop)$"] move scratchpad, move absolute position 100 100, resize set 500 600
bindsym $mod+e [class="^(skypeforlinux|TelegramDesktop)$"] scratchpad show


assign [instance="^MainTerminal$"] $ws1
assign [class="^Firefox$"]         $ws2
assign [instance="^wim$"]          $ws4

bindsym $mod+x exec $psst [ "$(i3-msg '[instance=MainTerminal] focus')" = '[{"success":true}]' ] || i3-msg exec "~/bin/term"
bindsym $mod+w exec $psst [ "$(i3-msg '[class=Firefox] focus')" = '[{"success":true}]' ] || i3-msg exec "firefox"
bindsym $mod+1 exec $psst [ "$(i3-msg '[instance=wim] focus')" = '[{"success":true}]' ] || i3-msg exec "~/bin/wim"
bindsym $mod+3 scratchpad show

bindsym $mod+Shift+b exec i3-input -F 'mark %s' -P 'Mark name: '
bindsym $mod+b exec i3-input -F '[con_mark=%s] focus' -P 'Go to mark: '

client.focused            $black       $black       $magenta    $darkblack
client.focused_inactive   $black       $black       $white      $darkblack
client.unfocused          $black       $black       $white      $darkblack
client.urgent             $black       $black       $red        $darkblack

floating_modifier $mod
focus_on_window_activation smart

bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Control+q kill

bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+colon move right

bindsym $mod+h split h
bindsym $mod+v split v

bindsym $alt+g exec rofi -show

bindsym $mod+q fullscreen toggle

bindsym $alt+Tab workspace back_and_forth

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

bindsym $mod+d focus child

# switch to workspace
bindsym $mod+4 workspace  $ws4
bindsym $mod+5 workspace  $ws5
bindsym $mod+6 workspace  $ws6
bindsym $mod+7 workspace  $ws7
bindsym $mod+8 workspace  $ws8
bindsym $mod+9 workspace  $ws9
bindsym $mod+0 workspace  $ws10
#bindsym $mod+0 workspace $ws11
#bindsym $mod+0 workspace $ws12

# # move focused container to workspace
# bindsym $mod+Shift+1 move container to workspace $ws1
# bindsym $mod+Shift+2 move container to workspace $ws2
# bindsym $mod+Shift+3 move container to workspace $ws3
# bindsym $mod+Shift+4 move container to workspace $ws4
# bindsym $mod+Shift+5 move container to workspace $ws5
# bindsym $mod+Shift+6 move container to workspace $ws6
# bindsym $mod+Shift+7 move container to workspace $ws7
# bindsym $mod+Shift+8 move container to workspace $ws8
# bindsym $mod+Shift+9 move container to workspace $ws9
# bindsym $mod+Shift+0 move container to workspace $ws10

#bindsym $mod+s [workspace=*] scratchpad show

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+r mode "resize"

bar {
    status_command    i3status
    position          bottom
    #mode              hide
    workspace_buttons yes
    tray_output       primary
    #binding_mode_indicator no
    i3bar_command i3bar -t
    
    #font: PragmataPro for Powerline 10, Icons 10

    colors {
        background #000000
        statusline #ffffff

        focused_workspace  #ffffff #000000
        active_workspace   #ffffff #000000
        inactive_workspace #888888 #000000
        urgent_workspace   #ffffff #900000
    }
}

mode "focused" {
    # hardcoded focus keybindings
    bindsym b [class="(?i)firefox"] focus, mode "default"
    bindsym w [class="(?i)terminal" title="weechat"] focus
    bindsym z [class="(?i)zathura"] focus

    # keybindings for marking and jumping to clients
    bindsym a exec i3-input -F 'mark %s' -P 'Mark name: '
    bindsym g exec i3-input -F '[con_mark=%s] focus' -P 'Go to mark: '

    # Assign marks to keys 1-5
    bindsym Shift+1 mark mark1
    bindsym Shift+2 mark mark2
    bindsym Shift+3 mark mark3
    bindsym Shift+4 mark mark4
    bindsym Shift+5 mark mark5

    # Jump to clients marked 1-5
    bindsym 1 [con_mark="mark1"] focus
    bindsym 2 [con_mark="mark2"] focus
    bindsym 3 [con_mark="mark3"] focus
    bindsym 4 [con_mark="mark4"] focus
    bindsym 5 [con_mark="mark5"] focus

    # Exit to the default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+n mode "focused"

mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # They resize the border in the direction you pressed, e.g.
    # when pressing left, the window is resized so that it has
    # more space on its left

    bindsym j resize shrink left 2 px or 2 ppt
    bindsym Shift+J resize grow   left 2 px or 2 ppt

    bindsym k resize shrink down 2 px or 2 ppt
    bindsym Shift+K resize grow   down 2 px or 2 ppt

    bindsym l resize shrink up 2 px or 2 ppt
    bindsym Shift+L resize grow   up 2 px or 2 ppt

    bindsym semicolon resize shrink right 2 px or 2 ppt
    bindsym Shift+colon resize grow   right 2 px or 2 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink left 2 px or 2 ppt
    bindsym Shift+Left resize grow   left 2 px or 2 ppt

    bindsym Down resize shrink down 2 px or 2 ppt
    bindsym Shift+Down resize grow   down 2 px or 2 ppt

    bindsym Up resize shrink up 2 px or 2 ppt
    bindsym Shift+Up resize grow   up 2 px or 2 ppt

    bindsym Right resize shrink right 2 px or 2 ppt
    bindsym Shift+Right resize grow   right 2 px or 2 ppt

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym Control+C mode "default"
}

bindsym $mod+Pause mode "$mode_system"


set $passthrough_keys Suspend all window manager keys exit: Mod+Shift+Backspace)
mode "$passthrough_keys" {
        bindcode Alt+Shift+22 mode "default"
}
bindcode Alt+Shift+22 mode "$passthrough_keys"

