#!/bin/zsh
file=/tmp/xprop_pipe
xprop=$(xprop WM_CLASS WM_NAME WM_WINDOW_ROLE WM_TRANSIENT_FOR _NET_WM_WINDOW_TYPE _NET_WM_STATE _NET_WM_PID -id $1)

echo "${xprop}" > "${file}" 
< "${file}" rofi -dmenu -location 6 -yoffset -24|xsel
