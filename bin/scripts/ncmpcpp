#!/bin/zsh
[[ -z $(pgrep mpd) ]] && mpd
[[ -z $(pgrep mpdscribble) ]] && mpdscribble --conf /home/neg/.config/mpdscribble/neg.conf

wid=$(xdotool search --classname mpd-pad2)
if [ -z "$wid" ]; then
    /bin/urxvt -fn xft:Pragmata\ Pro\ for\ Powerline:pixelsize=18 -name mpd-pad2 -e ncmpcpp &
fi
