#!/bin/zsh

function main(){
    pattern="$1"
    mpc --format "%position% %artist% %album% %title%" playlist \
        | ${BIN_HOME}/scripts/negrep ${pattern} \
        | awk '{print $1}'  \
        | mpc del
}

main "$@"
