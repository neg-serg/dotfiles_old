#!/bin/zsh

if [[ ${X11_WM} == "notion" ]]; then
    pkill -USR1 -x sxhkd
    pkill admiral
    pkill dzen2
    pkill rofi
    ~/bin/scripts/panels
    notionflux -e 'notioncore.restart()'
fi

if [[ ${X11_WM} == "windowchef" ]]; then
	$HOME/.config/windowchef/windowchefrc 
	pkill -USR1 -x sxhkd
fi
