#!/bin/zsh

menu=$(gpaste --oneline | rofi -font 'Pragmata Pro for Powerline bold 12' -yoffset -22 -location 6 -width 1850 -lines 10 -dmenu -p "[>>]")
selection=$(echo "$menu" | cut -d ':' -f1)
gpaste select $selection
